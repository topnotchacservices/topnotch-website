"use client";
import { useState } from "react";

export function AdminLogin({ configured }: { configured: boolean }) {
  const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(""); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ username, password }) }); if (response.ok) window.location.reload(); else { const result = await response.json(); setError(result.error ?? "Unable to sign in."); setLoading(false); } }
  return <main className="grid min-h-screen place-items-center bg-slate-100 p-5"><form onSubmit={submit} className="w-full max-w-md rounded-xl border border-slate-200 bg-white p-7 shadow-sm"><p className="text-xs font-extrabold tracking-[.16em] text-sky-700">TOP NOTCH WEBSITE</p><h1 className="mt-2 text-3xl font-black text-[#082544]">Admin sign in</h1>{configured ? <><p className="mt-3 text-sm leading-6 text-slate-600">Use the credentials configured in this website&apos;s private environment file.</p><label className="mt-6 block text-sm font-bold text-[#082544]">Username<input value={username} onChange={(event) => setUsername(event.target.value)} autoComplete="username" required className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5 outline-none focus:border-sky-500" /></label><label className="mt-4 block text-sm font-bold text-[#082544]">Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} autoComplete="current-password" required className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5 outline-none focus:border-sky-500" /></label>{error && <p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm font-bold text-red-700">{error}</p>}<button disabled={loading} className="mt-6 min-h-11 w-full rounded-full bg-sky-500 px-5 text-sm font-extrabold text-white transition hover:bg-sky-600 disabled:opacity-60">{loading ? "Signing in..." : "Sign in"}</button></> : <div className="mt-5 rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900">Admin login is not configured. Copy <code>.env.example</code> to <code>.env.local</code>, set a username, password hash and session secret, then restart the server. Editing remains disabled until then.</div>}</form></main>;
}
