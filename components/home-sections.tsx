import Link from "next/link";
import { ActionButton } from "@/components/site-shell";

export function Eyebrow({ children, light = false }: { children: React.ReactNode; light?: boolean }) { return <p className={`text-xs font-extrabold tracking-[.16em] ${light ? "text-sky-200" : "text-sky-600"}`}>{children}</p>; }
export function SectionTitle({ eyebrow, title, text }: { eyebrow: string; title: string; text?: string }) { return <div className="max-w-2xl"><Eyebrow>{eyebrow}</Eyebrow><h2 className="mt-3 font-serif text-3xl font-bold leading-tight text-[#082544] sm:text-4xl">{title}</h2>{text && <p className="mt-4 text-base leading-7 text-slate-600">{text}</p>}</div>; }
export function VisualPlaceholder({ label, className = "" }: { label: string; className?: string }) { return <div className={`relative overflow-hidden bg-gradient-to-br from-sky-400 via-sky-600 to-[#082544] ${className}`}><div className="absolute inset-0 opacity-40 [background-image:linear-gradient(135deg,transparent_45%,white_45%,transparent_46%)]" /><div className="absolute bottom-5 left-5 border-l-2 border-white pl-3 text-xs font-bold tracking-[.14em] text-white">{label}<span className="mt-2 block text-[10px] font-medium tracking-normal text-sky-100">PHOTO PLACEHOLDER</span></div></div>; }
export function CardLink({ href, title, tag, color }: { href: string; title: string; tag: string; color: string }) { return <Link href={href} className="group block overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm transition duration-200 hover:-translate-y-1 hover:border-sky-300 hover:shadow-lg focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-sky-500 active:translate-y-0"><VisualPlaceholder label={tag} className={`h-39 bg-gradient-to-br ${color}`} /><div className="flex items-center justify-between p-5"><h3 className="font-serif text-xl font-bold text-[#082544]">{title}</h3><span className="grid size-9 place-items-center rounded-full bg-sky-100 text-sky-700 transition group-hover:bg-sky-500 group-hover:text-white">+</span></div></Link>; }
export { ActionButton };
