import Link from "next/link";
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { ActionButton, PageHero, SiteFooter, SiteHeader } from "@/components/site-shell";
import { getSiteContent } from "@/lib/content-store";

type Props = { params: Promise<{ slug: string }> };
export async function generateMetadata({ params }: Props): Promise<Metadata> { const { slug } = await params; const area = (await getSiteContent()).serviceAreas.find((item) => item.slug === slug && item.enabled); return area ? { title: area.metaTitle, description: area.metaDescription } : {}; }
export default async function CityServiceAreaPage({ params }: Props) { const { slug } = await params; const area = (await getSiteContent()).serviceAreas.find((item) => item.slug === slug && item.enabled); if (!area) notFound(); return <><SiteHeader /><main><PageHero eyebrow={`${area.region.toUpperCase()} SERVICE`} title={area.cityTitle} description={area.intro} /><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><div className="grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><div><h2 className="text-3xl font-black text-[#082544]">Complete comfort services in {area.name}</h2><p className="mt-4 max-w-2xl leading-7 text-slate-600">Whether your system needs prompt attention or planned care, Top Notch offers straightforward information and professional service for {area.name} properties.</p><div className="mt-7 grid grid-cols-2 gap-3">{area.services.map((service) => <Link key={service} href={service === "AC Repair" ? "/ac-repair" : service === "Air Duct Cleaning" ? "/air-duct-cleaning" : service === "Dryer Vent Cleaning" ? "/dryer-vent-cleaning" : service === "AC Maintenance" ? "/ac-maintenance" : "/ac-services"} className="rounded-lg border border-slate-200 px-4 py-4 text-sm font-bold text-[#082544] transition hover:border-sky-400 hover:text-sky-700">{service}</Link>)}</div></div><aside className="rounded-xl bg-sky-50 p-7"><p className="text-sm font-bold text-slate-600">Licensed & Insured</p><p className="mt-2 text-2xl font-black text-[#082544]">CAC1824888</p><div className="mt-6 grid gap-3"><ActionButton phone>Call Now</ActionButton><ActionButton>Book Service</ActionButton></div></aside></div><section className="mt-16 border-t border-slate-200 pt-10"><h2 className="text-2xl font-black text-[#082544]">Questions from {area.name} customers</h2><div className="mt-5 grid gap-3 md:grid-cols-2"><p className="rounded-lg bg-slate-50 p-4 text-sm text-slate-600">Do you provide same-day service? Call us to check the next available service window for your area.</p><p className="rounded-lg bg-slate-50 p-4 text-sm text-slate-600">Do you work on homes and businesses? Yes. We support residential and commercial customers throughout South Florida.</p></div></section></section></main><SiteFooter /></>; }