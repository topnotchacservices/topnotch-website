import { ActionButton, PageHero, SiteFooter, SiteHeader } from "@/components/site-shell";
import { getSiteContent } from "@/lib/content-store";
export default async function ServiceAreasPage() { const content = await getSiteContent(); return <><SiteHeader /><main><PageHero eyebrow="SERVICE AREAS" title="Local service across Broward and South Florida." description="Top Notch AC Services is ready to help homeowners and businesses throughout the region." /><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{content.serviceAreas.map((area) => <div key={area} className="rounded-lg border border-sky-100 bg-sky-50 px-4 py-4 text-center text-sm font-bold text-[#082544]">{area}</div>)}</div><div className="mt-8"><ActionButton>Book Service</ActionButton></div></section></main><SiteFooter /></>; }
